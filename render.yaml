services:
  - type: web
    name: iotai-node
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/v1/network/stats
